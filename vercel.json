{
    "version":2, 
    "builds":[{
        "src":"./dist/index.ts",
        "use":"@now/node"
    }],

    "routes":[
        {
         "src":"/(.*)",
         "dest":"dist/index.ts"
        }
       
    ]
}